function SEED addSeed(SEED s1, SEED s2) {
    SEED seed = 0;
    uint card_pos = 0;

    for (uint i = 0; i < num_out_cards; ++i) {
        card_pos = i * card_size;
        seed{card_pos:card_size} = s1{card_pos:card_size} + s2{card_pos:card_size};
    }

    return seed;
}