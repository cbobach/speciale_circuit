#define int_size 6
#define max_int 63
#define array_size 3969
#define output_size (array_size * int_size)

typedef uint_t 0 INPUT
typedef uint_t output_size OUTPUT
typedef uint_t int_size int
typedef uint_t 12 long

#parties 2

#input 1 INPUT
#output 1 OUTPUT
#input 2 INPUT
#output 2 OUTPUT

function int[array_size] init(INPUT _, INPUT _) {
    int res[array_size];

    for (long i = 0; i < max_int; ++i) {
res[i] = 254;
/*
        for (long j = 0; j < max_int; ++j) {
        }
*/
    }

    return res;
}

/*
 * Finalizing output by writing array to bits
 * */
function OUTPUT finalize(int input[array_size]) {
    OUTPUT output = 0;

    for (long i = 0; i < array_size; ++i) {
        output = output << int_size;
        output{0:int_size} = input[i];
    }

    return output;
}

function void main() {
    int arr[array_size];
    arr = init(input1, input2);

    output1 = finalize(arr);
}
