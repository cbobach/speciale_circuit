#define card_size 6
#define num_out_cards 20
#define seed_size (card_size * num_out_cards)

typedef uint_t 9 uint
typedef uint_t seed_size SEED

#parties 2

#input 1 SEED
#output 1 SEED
#input 2 SEED
#output 2 SEED

#include "add_seed.wir"

function void main() {
    output1 = addSeed(input1, input2);
}